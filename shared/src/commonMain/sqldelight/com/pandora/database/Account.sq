CREATE TABLE Account (
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    email TEXT NOT NULL,
    username TEXT NOT NULL,
    password TEXT NOT NULL
);

insert:
INSERT INTO Account(id, name, email, username, password) VALUES (?, ?, ?, ?, ?);